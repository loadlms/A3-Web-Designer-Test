---
import '../assets/styles/global.css';

interface Props {
  title: string;
  children: any;
  rawHtml?: string;
}

const { title, children } = Astro.props;
---

<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  </head>
  <body class="template1 bg-gray-50">
    <header class="header">
      <div class="container mx-auto px-4">
        <nav class="flex justify-between items-center py-4">
          <div class="logo">
            <a href="/" class="text-2xl font-bold text-gray-900">Business Solutions</a>
          </div>
          <div class="nav-links">
            <a href="/" class="text-gray-600 hover:text-gray-900 px-4">Home</a>
            <a href="#" class="text-gray-600 hover:text-gray-900 px-4">Sobre</a>
            <a href="#" class="text-gray-600 hover:text-gray-900 px-4">Contato</a>
          </div>
        </nav>
      </div>
    </header>

    <main class="main-content">
      <div class="container mx-auto px-4 py-8">
        <section class="hero mb-12">
          <h1 class="text-4xl font-bold mb-6">{title}</h1>
        </section>

        <div class="mx-auto">
          {Astro.props.rawHtml && <Fragment set:html={JSON.parse(Astro.props.rawHtml)} />}
        </div>

        <section class="cta-section mt-12 text-center">
          <h2 class="text-3xl font-bold mb-4">Pronto para Começar?</h2>
          <p class="text-lg text-gray-600 mb-6">Junte-se a milhares de clientes satisfeitos hoje</p>
          <a href="#" class="inline-block bg-blue-600 text-white px-8 py-3 rounded-lg hover:bg-blue-700 transition-colors">
            Comece Agora
          </a>
        </section>
      </div>
    </main>

    <footer class="bg-gray-100 mt-16 py-8">
      <div class="container mx-auto px-4 text-center text-gray-600">
        <p>&copy; 2024 Business Solutions. Todos os direitos reservados.</p>
      </div>
    </footer>
  </body>
</html> 